{% extends 'base.html.twig' %}

{% block title %}Nowe hasło{% endblock %}

{% block body %}
    <h1>Ustaw nowe hasło</h1>

    {{ form_start(resetForm) }}
        {{ form_row(resetForm.plainPassword) }}
        <button class="btn btn-primary">Zapisz nowe hasło</button>
    {{ form_end(resetForm) }}
{% endblock %}
