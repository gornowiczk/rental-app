<h1>Resetowanie hasła</h1>

<p>Aby ustawić nowe hasło w aplikacji CarRental, kliknij poniższy link:</p>

<a href="{{ url('app_reset_password', {token: resetToken.token}) }}">{{ url('app_reset_password', {token: resetToken.token}) }}</a>

<p>Link wygaśnie za {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}.</p>

<p>Jeśli to nie Ty prosisz o reset hasła, zignoruj tę wiadomość.</p>
